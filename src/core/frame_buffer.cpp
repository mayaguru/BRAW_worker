#include "frame_buffer.h"

namespace braw {

// FrameBuffer currently keeps logic inline in the header. Implementation file exists
// so future optimisations (SIMD conversion, tiling) have a stable location.

}  // namespace braw
